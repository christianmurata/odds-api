overwrite: true
schema: "src/graphql"
documents: null
generates:
  graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
